# Capability policy — Document Processing Pack

Uses core agents **APXV-AGENT-001**, **APXV-AGENT-002**, **APXV-AGENT-003** (same as Reference Redaction Pack).

```bash
python -m scripts.apxv_ctl policy-verify
python -m scripts.apxv_ctl capabilities
```