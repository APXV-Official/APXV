# Capability policy — Document Processing Pack

Uses core agents **APX-AGENT-001**, **APX-AGENT-002**, **APX-AGENT-003** (same as Reference Redaction Pack).

```bash
python -m scripts.apx_ctl policy-verify
python -m scripts.apx_ctl capabilities
```