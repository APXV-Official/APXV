# APXV1 v1.1 — Release checklist

**Target:** v1.1.0 with voice suite, Tier A/B ceremony transparency, and honest public documentation.  
**Tag:** `v1.1.0` (on `main`; repository visibility remains **operator-controlled** until you set it public).

---

## Launch tiers (what you may claim publicly)

| Tier | Ceremony stance | Voice | Appropriate public claim |
|------|-----------------|-------|--------------------------|
| **v1.0.1** | Single-party honest setup, documented | Circuit only (`voice-redaction` in `apx-zk`) | Technical preview |
| **v1.1.0** | Signed transcript + verifier bundle when signing keys exist (Tier A/B) | Full STT → redact → attest → verify path | v1.1.0 release (not MPC) |
| **v1.2+** (optional) | MPC / multi-party ceremony (Tier C) | Production STT/TTS backends | Future — do not claim today |

See [cryptography/CEREMONY.md](cryptography/CEREMONY.md) for ceremony tiers and transcript format.

---

## P0 — Engineering complete

### Ceremony (Tier A/B)

- [x] Generate transcript after trusted setup:
  ```bash
  python -m scripts.ceremony_transcript --write --tier B --note "v1.1.0 release"
  python -m scripts.ceremony_transcript --verify
  ```
- [x] Export verifier bundle (VKs + manifests only, no PKs):
  ```bash
  python -m scripts.export_verifier_bundle --out dist/apxv1-verifier-bundle-v1.1.0
  ```
- [x] Third party can verify attestation with `verify_attestation --real-zk` (self-host or operator VKs)
- [x] `apx_doctor` reports ceremony transcript OK when present

### Voice suite

- [x] `agents/voice/` — STT/TTS providers + simulated backends for CI
- [x] `VoicePrivacyPipeline` + `voice-redaction` in entity ZK bridge
- [x] `tests/test_voice_suite.py`, `tests/test_voice_e2e.py`
- [x] v1.1 narrative: [DEMO-SCRIPT-V1.1.md](DEMO-SCRIPT-V1.1.md) (canonical scripted demo; video is v1.0.x text/E2EE only)

### Docs & CI

- [x] `SECURITY.md`, `ASSUMPTIONS.md`, README trust model updated
- [x] `CHANGELOG.md` v1.1.0 section complete
- [x] `docs/cryptography/CIRCUITS.md` — attest-path vs crate inventory
- [x] Doc sync: QUICKSTART, PROJECT-OVERVIEW, VERIFICATION, SECURITY-ARCHITECTURE
- [x] `docs/archive/README.md` — archive marked historical (not authoritative)
- [x] Ubuntu CI green (pytest + ceremony + voice)

### Release gate

```bash
cargo build --release --manifest-path rust/Cargo.toml -p apx-circuits -p apx-zk
cargo test -p apx-circuits -p apx-zk -q
pip install -e ".[dev,voice]"
python -m scripts.setup_first_run
python -m scripts.ceremony_transcript --write --tier B
python -m scripts.ceremony_transcript --verify
python -m scripts.apx_doctor
python -m scripts.apx_ctl integrity
python -m pytest tests/ -q --tb=short
```

- [x] Git tag `v1.1.0` on `main`
- [ ] GitHub **Release** page published with `apxv1-verifier-bundle-v1.1.0.zip` (use `scripts/publish_github_release.py` or UI)
- [ ] Repository set to **public** (operator choice, separate from tag)

---

## Sign-off

| Role | Status |
|------|--------|
| Engineering | [x] Tests + CI + tag |
| Security / accuracy | [x] Limits documented; no MPC/HIPAA overclaims |
| Demo / narrative | [x] Script canonical; video labeled v1.0.x |
| Public visibility | [ ] Operator enables when ready |