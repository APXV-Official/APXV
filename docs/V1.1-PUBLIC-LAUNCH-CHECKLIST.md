# APXV1 v1.1 — Public Launch Checklist

**Target:** Public launch with hardened ops, transparent ZK ceremony (Tier A/B), and voice privacy suite.  
**Release tag:** `v1.1.0` (do not push until all **P0** items are green and operator approves).

---

## Launch tiers (what you promise publicly)

| Tier | Ceremony stance | Voice | Public claim |
|------|-----------------|-------|--------------|
| **v1.0.1** (shipped) | Single-party honest setup, documented | Circuit only (`voice-redaction` in `apx-zk`) | Technical preview |
| **v1.1.0** (launch bar) | Signed ceremony transcript + verifier bundle (Tier A/B) | Full STT → redact → attest → verify path | Public launch |
| **v1.2+** (optional) | MPC / multi-party ceremony (Tier C) | Production STT/TTS backends | Enterprise / crypto-native |

See [cryptography/CEREMONY.md](cryptography/CEREMONY.md) for ceremony tiers and transcript format.

---

## P0 — Launch blockers

### Ceremony (Tier A/B)

- [ ] Generate transcript after trusted setup:
  ```bash
  python -m scripts.ceremony_transcript --write --tier B --note "v1.1.0 release"
  python -m scripts.ceremony_transcript --verify
  ```
- [ ] Export verifier bundle (VKs + manifests only, no PKs):
  ```bash
  python -m scripts.export_verifier_bundle --out dist/apxv1-verifier-bundle
  ```
- [ ] Third party can verify attestation with bundle + `verify_attestation --real-zk`
- [ ] `apx_doctor` reports ceremony transcript OK when present

### Voice suite

- [x] `agents/voice/` — STT/TTS providers + simulated backends for CI
- [x] `VoicePrivacyPipeline` + `voice-redaction` in entity ZK bridge
- [x] `tests/test_voice_suite.py`, `tests/test_voice_e2e.py`
- [ ] Re-record demo per [DEMO-SCRIPT-V1.1.md](DEMO-SCRIPT-V1.1.md)

### Docs & CI

- [x] `SECURITY.md`, `ASSUMPTIONS.md`, README trust model updated
- [x] `CHANGELOG.md` v1.1.0 section complete
- [x] `docs/cryptography/CIRCUITS.md` — attest-path vs crate inventory
- [x] Doc sync: QUICKSTART, PROJECT-OVERVIEW, VERIFICATION, SECURITY-ARCHITECTURE
- [ ] Ubuntu CI green (pytest + ceremony + voice)

### Release gate

```bash
cargo build --release --manifest-path rust/Cargo.toml -p apx-circuits -p apx-zk
cargo test -p apx-circuits -p apx-zk -q
pip install -e ".[dev,voice]"
python -m scripts.setup_first_run
python -m scripts.ceremony_transcript --write --tier B
python -m scripts.ceremony_transcript --verify
python -m scripts.apx_doctor
python -m scripts.apx_ctl integrity
python -m pytest tests/ -q --tb=short
```

- [ ] Tag `v1.1.0` and GitHub Release with verifier bundle artifact (operator approval required)

---

## Sign-off

| Role | P0 complete |
|------|-------------|
| Engineering | [ ] |
| Security review | [ ] |
| Demo / narrative | [ ] |
| Public launch (push approved) | [ ] |